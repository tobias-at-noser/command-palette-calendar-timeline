# AI Container Instructions

These instructions apply only when OpenCode runs in the container environment defined by `.ai-tools`.

## Jira

- Jira issues are available through the authenticated GitKraken CLI integration. Read an issue with `gk issue show jira <ISSUE-KEY> --with-comments --output json`.
